//Q1. Programa para calcular a área de um retângulo dados os comprimentos de duas arestas

/*const areaRet = (x) => (y) => x*y

const Res1 = areaRet (5) (4)
const Res2 = areaRet (3) (3)
const Res3 = areaRet (2) (2)

console.log(Res1)
console.log(Res2)
console.log(Res3)*/

//Defina funções para calcular o quadrado, o cubo e a raiz quadrada de um número passado como argumento reaproveitando uma definição de função genérica chamada expoente.

/*const expoente = (e) => (x) => x**e

const raizQ = expoente (1/2) (9)
const quadrado = expoente (2) (2)
const cubo = expoente (3) (2)

console.log(raizQ)
console.log(quadrado)
console.log(cubo)*/

//Função como argumento

/*const areaRet = (x,y) => x*y
const areaCirc = (x) => x**2

const exec = (f,x,y) => f(x,y)

const arearet = exec(areaRet,4,5)
const areacirc = exec(areaCirc,2)

console.log(arearet)
console.log(areacirc)*/

//Função anônima

